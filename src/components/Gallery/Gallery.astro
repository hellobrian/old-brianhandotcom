<!-- ---
const { slug, keycaps } = Astro.props;
const allKeyboardImages = await Astro.glob("/images/keyboards/**/*.png");
const filteredImages = allKeyboardImages
  .map((imageObj) => imageObj.default)
  .filter((str: string) => str.includes(slug))
  .filter((str: string) => str.includes(keycaps));

const mainImageStr = filteredImages.filter((str) => str.includes("main"));
const sortedImages = new Set([...mainImageStr, ...filteredImages]);

import Gallery from "./Gallery";
---

<Gallery images={[...sortedImages]} client:load /> -->
